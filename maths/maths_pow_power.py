def maths_pow():
    # Find the power of given number
    x = 3
    y = 6
    z = pow(x, y)
    print(f'The value of {x} to the power of {y} is {z}')

if __name__=='__main__':
    maths_pow()

